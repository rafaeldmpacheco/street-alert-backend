* Create database schema running the file `backend/database.sql`

1. `npm install`
1. `npm install -g gulp`
1. `npm run build`
1. `npm start`
1. `npm run watch` for watch source changes

Runs on localhost:3000

Default system user/password: admin1/senha